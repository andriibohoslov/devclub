#include <stdio.h>

int main() {
    int first, step, count;

    scanf("%d %d %d", &first, &step. &count);

    for ( ; count >= 1; count --) {
        first += step;
        printf("%d ", );
    }

    return 0;
}
