#include <stdio.h>
unsigned average(unsigned int a, unsigned int b) {
    return a / 2 + a / 2 + (a % 2 + b % 2) / 2;
}

int main() {

unsigned int a = 4000000001;
unsigned int b = 4000000001;
unsigned int avrg

average(a, b);

printf("%u\n", avrg);

    return 0;
}
