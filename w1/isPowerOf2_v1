#include <stdio.h>

int main() {
    int a;

    scanf("%d", &a);

    for ( ; a / 2 >= 1; a /= 2 ) {
        if ( a % 2 != 0 ) {
            printf("no\n");
            break;
        } else if ( a / 2 == 1 && a % 2 == 0 ) {
            printf("yes\n");
        }
    }
    return 0;
}
